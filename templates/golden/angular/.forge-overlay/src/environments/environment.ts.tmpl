// Environment-owned wiring. The scaffold-time default is overridable per
// deployment by swapping this file (fileReplacements) — not by editing code.
export const environment = {
  apiBaseUrl: '{{.APIBaseURL}}',
};
